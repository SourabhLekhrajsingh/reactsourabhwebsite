import React from 'react';
import { ToastContainer, toast } from 'react-toastify';

 const Homing = () =>{
        toast("this is my first message");
    }  ;
export const Homing = () => {
    return (
        <div>
            <button onClick={Homing}>
            first react button
            </button>
        </div>
    )
}
    